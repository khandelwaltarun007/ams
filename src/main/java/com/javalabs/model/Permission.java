package com.javalabs.model;

import java.util.HashSet;
import java.util.Set;

import jakarta.persistence.Entity;
import jakarta.persistence.FetchType;
import jakarta.persistence.GeneratedValue;
import jakarta.persistence.GenerationType;
import jakarta.persistence.Id;
import jakarta.persistence.ManyToMany;
import jakarta.persistence.Table;
import lombok.Data;
import lombok.NoArgsConstructor;

/*@Data
@Table(name="permission")
@Entity
@NoArgsConstructor*/
public class Permission {
	/*
	 * @Id
	 * 
	 * @GeneratedValue(strategy = GenerationType.IDENTITY) private Long id; private
	 * String name;
	 * 
	 * @ManyToMany(mappedBy = "permissions", fetch = FetchType.EAGER) private
	 * Set<Role> roles = new HashSet<>();
	 * 
	 * public Permission(String name) { this.name = name; }
	 */
    
    
}
